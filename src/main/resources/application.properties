spring.application.name=AuthApp

# MySQL driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JDBC URL using environment variables if set, otherwise fallback values
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:sql12.freesqldatabase.com}:${MYSQL_PORT:3306}/${MYSQL_DB:sql12800149}?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
spring.datasource.username=${MYSQL_USER:sql12800149}
spring.datasource.password=${MYSQL_PASSWORD:PJznT7VhCY}

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Use dynamic port from Render, fallback to 9898
server.port=${PORT:9898}

logging.level.org.springframework.security=DEBUG
logging.level.your.package.name=DEBUG
